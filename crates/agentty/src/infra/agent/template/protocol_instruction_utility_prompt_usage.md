For this one-shot utility prompt, return the entire response as a JSON object like `{"answer":"...","questions":[],"follow_up_tasks":[],"summary":null}`. Put all useful plain-text output in `answer`.
