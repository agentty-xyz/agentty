Keep session-turn output in one JSON object using the bootstrapped `answer`,
`questions`, and `review_comment_outcomes` fields. Keep Mermaid in `answer` as
unindented ```` ```mermaid ```` blocks, never as plain text, indented code, or fences
lacking the `mermaid` info string.
