name: Presubmit

on:
  pull_request:
  merge_group:
    types: [checks_requested]

permissions:
  contents: read

jobs:
  validate:
    uses: ./.github/workflows/rust-validation.yml
    with:
      cargo-audit: true
      workspace-tests: true
